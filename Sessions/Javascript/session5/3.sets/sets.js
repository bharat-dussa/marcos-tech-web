/**
 * A JavaScript Set is a collection of unique values.
 * Each value can only occur once in a Set.
 * A Set can hold any value of any data type.
 */

/**
 *  new Set() - It creates a instance of Set Object and we can use functionality of that Objects
 *  So what is use of Set, we can use Array also right, well, it creates unique values it doesn't duplicate the values.
 * keys(),
 * values(),
 * entries(),
 * add(),
 * delete(),
 * has(),
 * clear(),
 * forEach(),
 */

const names = new Set([
  "bharat",
  "sai",
  "vamshi",
  "aman",
  "tirumalesh",
  "tejas",
  "tejas",
]);



// console.log('names:', names);

